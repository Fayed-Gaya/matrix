# Shared Assets

Assets in this directory are intentionally shared by multiple levels.

Keep level-specific art, audio, maps, and tilesets inside that level's own
`assets/` directory. Move assets here only when more than one level is expected
to load the same source file at runtime.
