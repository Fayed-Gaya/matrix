# Untitled Assets

Gameplay maps are currently authored and loaded from the repository-level
`Tiled/` workspace. This directory still holds runtime assets that are not part
of the Tiled map source:

The opening screen uses:

- `backgrounds/environment_forestbackground.png`
- `audio/forest_ambience.mp3`

Character prototype sheets now live in:

- `sprites/player_snoblin.png`
- `sprites/npc_snoblin_blue.png`
- `sprites/static_shadow.png`

Put sprites, real tilesets, audio, fonts, and additional Tiled files for
Untitled Game in this directory as the level grows. Preserve downloaded license
and attribution files next to the assets they describe.
